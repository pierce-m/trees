/* The verticle space between a node and its parent/child. */
var VERT_SPACE = 20;

/* Controls how 'bunched up' children of a node are. */
var NODE_SPACING = 10;

/* Offset of text inside svg circle. */
var TEXT_OFFSET = 4.5;

/* Slice of time between transitions. Can be used to speed up or slow down
 * visualized operations */
var TRANSITION_SPEED = 1000;
